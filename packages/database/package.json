{
  "name": "@clippy/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "supabase gen types typescript --local > src/types.ts",
    "migrate": "supabase db push",
    "seed": "tsx src/seed.ts",
    "reset": "supabase db reset",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --fix",
    "test": "vitest"
  },
  "dependencies": {
    "@clippy/shared": "workspace:*",
    "@supabase/supabase-js": "^2.39.0",
    "drizzle-orm": "^0.29.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.0",
    "tsx": "^4.7.0",
    "vitest": "^1.0.0"
  }
}
