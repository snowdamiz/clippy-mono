{
  "name": "@clippy/cloudflare-edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --fix",
    "clean": "rimraf dist .wrangler"
  },
  "dependencies": {
    "@clippy/shared": "workspace:*",
    "@cloudflare/ai": "^1.0.0",
    "@supabase/supabase-js": "^2.39.0",
    "hono": "^3.11.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "wrangler": "^3.22.0"
  }
}
